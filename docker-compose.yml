networks:
  traefik:
    external: true
  appnet:
    driver: bridge

volumes:
  # Downloaded Scryfall card images. A named volume rather than a bind mount:
  # Docker seeds an empty named volume from the image's /data/images, ownership
  # included, so it comes up owned by the backend's uid 10001 with no privileged
  # chown step. A bind mount would be created root:root and the backend could not
  # write to it. Survives `docker compose down` — only `down -v` clears it.
  card-images:

services:
  db:
    image: postgres:16-alpine
    container_name: mtg-cube-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB is required}
    volumes:
      - /pwspool/software/mtg-meta-tracker/postgres:/var/lib/postgresql/data
    networks:
      - appnet
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:?POSTGRES_USER is required} -d ${POSTGRES_DB:?POSTGRES_DB is required}"]
      interval: 5s
      timeout: 3s
      retries: 10

  backend:
    build:
      context: ./backend
    container_name: mtg-cube-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      HTTP_ADDR: ":8080"
      DATABASE_URL: postgres://${POSTGRES_USER:?POSTGRES_USER is required}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@db:5432/${POSTGRES_DB:?POSTGRES_DB is required}?sslmode=disable
      IMAGE_CACHE_DIR: /data/images
    volumes:
      - card-images:/data/images
    depends_on:
      db:
        condition: service_healthy
    networks:
      - appnet

  frontend:
    build:
      context: .
      args:
        BACKEND_ORIGIN: http://backend:8080
    container_name: mtg-cube-frontend
    restart: unless-stopped
    environment:
      BACKEND_ORIGIN: http://backend:8080
      REVALIDATE_SECRET: ${REVALIDATE_SECRET:?REVALIDATE_SECRET is required}
    depends_on:
      - backend
    networks:
      - appnet
      - traefik
    labels:
      - traefik.enable=true
      - traefik.http.routers.mtg-frontend.rule=Host(`cube.fullcontrolmtg.com`)
      - traefik.http.routers.mtg-frontend.tls=true
      - traefik.http.routers.mtg-frontend.tls.certresolver=lets-encrypt
      - traefik.http.services.mtg-frontend.loadbalancer.server.port=3000
